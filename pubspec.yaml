name: railpk
description: "Overlay PK ferroviaire temps réel"
publish_to: 'none'
version: 2.1.5+9

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - data/rail_record_referentiel_pk_france.geojson
